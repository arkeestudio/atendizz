# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Environment variables / secrets — NUNCA commitar (contêm chaves da Evolution, Google, Supabase)
.env
.env.*
!.env.example

# Schema concatenado gerado localmente (a fonte de verdade é supabase/migrations/)
dev-schema.sql

# Settings locais do Claude Code (allowlist pode conter paths/chaves de teste)
.claude/settings.json
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
